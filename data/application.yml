instance:
    clientUrl: ${CLIENTURL:-http://localhost:8088}
    
repository:
    type: 5
    blazegraph:
        url: http://blazegraph:8080/blazegraph
        
ping:
    enabled: true
    endpoint: http://bibbox-sys-commander-master-fdp-client
    interval: 1800000 # milliseconds (only for test increase in production)


